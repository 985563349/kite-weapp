<wxs src="../shared/wxs/utils.wxs" module="utils" />
<wxs src="./flex.wxs" module="computed" />

<view
  id="{{ kId }}"
  style="{{ computed.flexStyle({ gap, style }) }}"
  class="k-class {{ computed.flexClass({justify, wrap, gap, align, vertical}) }} "
>
  <slot />
</view>
