<wxs src="./col.wxs" module="computed" />

<view
  id="{{ kId }}"
  class="{{ computed.colClass({ span, offset, pull, push, order, xs, sm, md, lg, xl }) }} k-class"
  style="{{ computed.colStyle({ gutter, flex, style }) }}"
>
  <slot />
</view>
