<wxs src="../shared/wxs/utils.wxs" module="utils" />
<wxs src="./space.wxs" module="computed" />

<view
  id="{{ kId }}"
  style="{{ computed.spaceStyle({ size, style }) }}"
  class="{{ computed.spaceClass({ size, align, vertical, wrap }) }} k-class"
>
  <slot />
</view>
