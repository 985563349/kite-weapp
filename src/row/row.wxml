<wxs src="../shared/wxs/utils.wxs" module="utils" />
<wxs src="./row.wxs" module="computed" />

<view
  id="{{ kId }}"
  style="{{ computed.rowStyle({ gutter, style }) }}"
  class="{{ utils.bem('k-row', [justify, align, { wrap }]) }} k-class"
>
  <slot />
</view>
