<k-transition
  id="{{ tId }}"
  visible="{{ visible }}"
  k-class="k-class k-overlay"
  style="z-index: {{ zIndex }}; {{ style }}"
  duration="{{ duration }}"
  bind:tap="onTap"
  catch:touchmove="{{ lockScroll ? 'noop' : '' }}"
  aria-role="{{ ariaRole || 'button' }}"
  aria-label="{{ ariaLabel || 'close' }}"
>
  <slot />
</k-transition>
