<wxs src="../shared/wxs/utils.wxs" module="utils" />

<view wx:if="{{ title }}" class="{{ utils.bem('k-cell-group__title', [variant]) }} k-title-class"> {{ title }} </view>

<view id="{{ kId }}" style="{{ style }}" class="{{ utils.bem('k-cell-group', [variant]) }} k-class">
  <slot />
</view>
